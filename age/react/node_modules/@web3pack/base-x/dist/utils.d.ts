/**
 * Alias for all buffer like types.
 */
export declare type BufferLike = Uint8Array | number[];
/**
 * Calculate factoriel of n.
 */
export declare const calculateFactor: (x: number, y?: number) => number;
/**
 * Convert a Uint8Array to a hex string.
 *
 * Same as below:
 *  const buffer = Buffer.from('0x010203040506', 'hex');
 */
export declare const uint8ToHex: (uint8: Uint8Array) => string;
/**
 * Convert a hex string to a Uint8Array.
 *
 * Same as below:
 *  const hexString = Buffer.from(uint8array).toString('hex');
 */
export declare const uint8FromHex: (hex: string) => Uint8Array | never;
/**
 * Convert a BufferLike object to a Uint8Array.
 */
export declare const uint8FromBufferLike: (buffer: BufferLike) => Uint8Array;
