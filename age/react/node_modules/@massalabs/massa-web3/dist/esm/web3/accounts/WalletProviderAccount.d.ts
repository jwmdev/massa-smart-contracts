/// <reference types="node" />
import { IBaseAccount } from '../../interfaces/IBaseAccount';
import { IAccount } from '@massalabs/wallet-provider';
import { ISignature } from '../../interfaces/ISignature';
import { IRollsData } from '../../interfaces/IRollsData';
import { ITransactionData } from '../../interfaces/ITransactionData';
import { ICallData } from '../../interfaces/ICallData';
import { IContractData } from '../../interfaces/IContractData';
export declare class WalletProviderAccount implements IBaseAccount {
    private account;
    constructor(account: IAccount);
    sign(data: Buffer): Promise<ISignature>;
    address(): string;
    sellRolls(rollsData: IRollsData): Promise<string>;
    buyRolls(rollsData: IRollsData): Promise<string>;
    sendTransaction(txData: ITransactionData): Promise<string>;
    callSmartContract(callData: ICallData): Promise<string>;
    deploySmartContract(_: IContractData): Promise<string>;
    verify(): Promise<void>;
}
