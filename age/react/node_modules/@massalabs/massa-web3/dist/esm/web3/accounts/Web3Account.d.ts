/// <reference types="node" />
import { IAccount } from '../../interfaces/IAccount';
import { IBaseAccount } from '../../interfaces/IBaseAccount';
import { IPublicApiClient } from '../../interfaces/IPublicApiClient';
import { IRollsData } from '../../interfaces/IRollsData';
import { ISignature } from '../../interfaces/ISignature';
import { BaseClient } from '../BaseClient';
import { ICallData } from '../../interfaces/ICallData';
import { IContractData } from '../../interfaces/IContractData';
export declare class Web3Account extends BaseClient implements IBaseAccount {
    private account;
    private publicApiClient;
    constructor(account: IAccount, publicApiClient: IPublicApiClient);
    verify(): Promise<void>;
    sign(data: Buffer): Promise<ISignature>;
    address(): string;
    sellRolls(txData: IRollsData): Promise<string>;
    buyRolls(txData: IRollsData): Promise<string>;
    sendTransaction(txData: IRollsData): Promise<string>;
    callSmartContract(callData: ICallData): Promise<string>;
    deploySmartContract(contractData: IContractData): Promise<string>;
}
