import * as tailwindcss_types_config from 'tailwindcss/types/config';

interface MaybeNested<K extends keyof any = string, V = string> {
    [key: string]: V | MaybeNested<K, V>;
}
declare const SCHEME: unique symbol;
type Colors = MaybeNested<string, string>;
interface ColorsWithScheme<T> extends Colors {
    [SCHEME]?: T;
}
type SchemerFn<T> = (colors: Colors) => ColorsWithScheme<T>;
type HslaArray = [number, number, number, number | undefined];
declare const toHslaArray: (colorValue?: string) => HslaArray;
type ConfigObject = Record<string, ColorsWithScheme<'light' | 'dark'>>;
type ConfigFunction = ({ light, dark, }: {
    light: SchemerFn<'light'>;
    dark: SchemerFn<'dark'>;
}) => ConfigObject;
interface Options {
    cssVariablePrefix?: string;
    cssVariableSuffix?: string;
}
declare const resolveConfig: (config?: ConfigObject | ConfigFunction, { cssVariablePrefix, cssVariableSuffix }?: Options) => {
    variants: {
        name: string;
        definition: string[];
    }[];
    utilities: Record<string, Record<string, string>>;
    colors: Record<string, ({ opacityValue, opacityVariable, }: {
        opacityValue: string;
        opacityVariable: string;
    }) => string>;
};
declare const createThemes: (config?: ConfigObject | ConfigFunction, options?: Options) => {
    handler: tailwindcss_types_config.PluginCreator;
    config?: Partial<tailwindcss_types_config.Config> | undefined;
};

export { Colors, ColorsWithScheme, ConfigFunction, ConfigObject, Options, createThemes, resolveConfig, toHslaArray };
